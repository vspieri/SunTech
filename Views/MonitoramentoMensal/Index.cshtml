@model IEnumerable<SunTech.Models.MonitoramentoMensal>

@{
    ViewData["Title"] = "Index";
}

<h1>Monitoramento Mensal</h1>

<p>
    <a asp-action="Create" class="btn1">Cadastrar</a>
</p>
<table class="table">
    <thead>
                <div>
            @using (Html.BeginForm("Index", "MonitoramentoMensal", FormMethod.Get))
            {
                <p>
                    Digite aqui sua pesquisa: @Html.TextBox("pesquisa")
                    <input class="btn btn-primary btn2" type="submit" value="Pesquisar" />
                </p>
            }
</div>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Mes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MediaMensal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Monitoramento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Mes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MediaMensal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Monitoramento.Id)
            </td>
            <td>
                <a class="btn5" asp-action="Edit" asp-route-id="@item.Id">Editar</a> |
                <a class="btn5" asp-action="Details" asp-route-id="@item.Id">Detalhes</a> |
                <a class="btn5" asp-action="Delete" asp-route-id="@item.Id">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
